<UserControl x:Class="GameStudioClicker.Wpf.Views.AchievementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="AchievementProgressBarStyle"
               TargetType="{x:Type ProgressBar}">
            <Setter Property="Height"
                    Value="8" />
            <Setter Property="Background"
                    Value="{StaticResource GameBackgroundBrush}" />
            <Setter Property="Foreground"
                    Value="{StaticResource PrimaryCyanBrush}" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ProgressBar}">
                        <Grid ClipToBounds="True">
                            <Border x:Name="PART_Track"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="4" />

                            <Border x:Name="PART_Indicator"
                                    HorizontalAlignment="Left"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="4" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEarned}"
                             Value="True">
                    <Setter Property="Foreground"
                            Value="{StaticResource SuccessBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border Margin="1"
                Padding="1"
                CornerRadius="8"
                Background="{StaticResource GameBackgroundBrush}"
                SnapsToDevicePixels="True">

            <ItemsControl ItemsSource="{Binding Achievements}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel ItemWidth="176"
                                   ItemHeight="176" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- One square achievement tile -->
                        <Border Margin="4"
                                Padding="12"
                                CornerRadius="6"
                                Background="{StaticResource SurfaceBrush}"
                                BorderThickness="1">

                            <Border.Style>
                                <Style TargetType="{x:Type Border}">
                                    <!-- Locked appearance -->
                                    <Setter Property="BorderBrush"
                                            Value="{StaticResource BorderBrush}" />
                                    <Setter Property="Opacity"
                                            Value="0.6" />

                                    <Style.Triggers>
                                        <!-- Earned appearance -->
                                        <DataTrigger Binding="{Binding IsEarned}"
                                                     Value="True">
                                            <Setter Property="BorderBrush"
                                                    Value="{StaticResource SuccessBrush}" />
                                            <Setter Property="Opacity"
                                                    Value="1" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <!-- Achievement name -->
                                    <RowDefinition Height="Auto" />

                                    <!-- Description -->
                                    <RowDefinition Height="*" />

                                    <!-- Progress and status -->
                                    <RowDefinition Height="Auto" />

                                    <!-- Progress bar -->
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- Always-visible achievement name -->
                                <TextBlock Grid.Row="0"
                                           Text="{Binding DisplayName}"
                                           FontSize="15"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource PrimaryTextBrush}"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap" />

                                <!-- Achievement objective -->
                                <TextBlock Grid.Row="1"
                                           Text="{Binding Description}"
                                           Margin="0,8"
                                           VerticalAlignment="Center"
                                           FontSize="12"
                                           Foreground="{StaticResource SecondaryTextBrush}"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap" />

                                <!-- Progress amount and earned status -->
                                <Grid Grid.Row="2"
                                      Margin="0,0,0,6">

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding ProgressText}"
                                               VerticalAlignment="Center"
                                               FontSize="11"
                                               Foreground="{StaticResource SecondaryTextBrush}" />

                                    <TextBlock Grid.Column="1"
                                               VerticalAlignment="Center"
                                               FontFamily="{StaticResource DisplayFontFamily}"
                                               FontSize="7">

                                        <TextBlock.Style>
                                            <Style TargetType="{x:Type TextBlock}">
                                                <Setter Property="Text"
                                                        Value="LOCKED" />
                                                <Setter Property="Foreground"
                                                        Value="{StaticResource SecondaryTextBrush}" />

                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEarned}"
                                                                 Value="True">
                                                        <Setter Property="Text"
                                                                Value="EARNED" />
                                                        <Setter Property="Foreground"
                                                                Value="{StaticResource SuccessBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>

                                <ProgressBar Grid.Row="3"
                                             Minimum="0"
                                             Maximum="{Binding RequirementValue, Mode=OneWay}"
                                             Value="{Binding CurrentProgress, Mode=OneWay}"
                                             Style="{StaticResource AchievementProgressBarStyle}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>
</UserControl>